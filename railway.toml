[build]
builder = "RAILPACK"
buildCommand = "pnpm db:generate && pnpm --filter api build"

[deploy]
startCommand = "pnpm --filter api start:prod"
healthcheckPath = "/api/v1/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
